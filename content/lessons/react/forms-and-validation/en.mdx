---
title: "Forms and validation"
description: "Controlled inputs, parsing, and messaging."
level: junior
order: 8
---

# Forms and validation

In forms you usually need to:

- control inputs (`value` + `onChange`)
- parse (e.g. string → number)
- validate and show errors

Rule of thumb: **validate parsed data**, not raw strings.

