---
title: "`keyof` + `typeof` in practice"
description: "Types from existing values."
level: "mid"
order: 14
---

# `keyof` + `typeof` in practice

`typeof config` captures an object shape; `keyof` lists allowed keys.

Avoid duplicating magic strings: derive unions from the source of truth.

## Recursos

- MDN Web Docs
- Documentación oficial del tema cuando aplique
