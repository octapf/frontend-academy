---
title: "Index access types (`T['k']`)"
description: "Extract property types."
level: "mid"
order: 21
---

# Index access types (`T['k']`)

`User['id']` returns that property’s type; combine with `keyof` for value unions.

Useful when backends return a documented subset.

## Recursos

- MDN Web Docs
- Documentación oficial del tema cuando aplique
