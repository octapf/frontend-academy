---
title: "Bundler type resolution"
description: "`exports`, `types`, conditions."
level: "mid"
order: 25
---

# Bundler type resolution

Modern packages expose `exports` with separate `types` paths.

If resolution fails, inspect `package.json` and `moduleResolution` settings.

## Recursos

- MDN Web Docs
- Documentación oficial del tema cuando aplique
